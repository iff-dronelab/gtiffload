[
    {"name":"gtifPath",
     "label": "GTiff Image Path",
     "description": "Input path of the Gtiffs",
     "type": "string",
     "default": "",
     "group": "General"
   },
   {"name":"gtifoutPath",
    "label": "GTiff Output Path",
    "description": "Output path of the Gtiffs",
    "type": "string",
    "default": "",
    "group": "General"
  },
  {"name":"translatedPath",
   "label": "GTiff Translated Path",
   "description": "Translated path of the Gtiffs",
   "type": "string",
   "default": "",
   "group": "General"
 },
   {"name":"refreshInterval",
    "label": "Load Refresh Interval",
    "description": "Refresh Interval for loading GTiff",
    "type": "number",
    "default": 10,
    "group": "General"
   }
   ,
    {
      "name":"camera",
     "label": "Camera option",
     "description": "Choose the camera used for taking the images.",
     "type": "choice",
     "default": "flir",
     "options":["flir", "p4p", "p4p_lowres"],
     "group": "General"
    }

]


