[
    {"name":"gtifPath",
     "label": "GTiff Image Path",
     "description": "Input path of the Gtiffs",
     "type": "string",
     "default": "",
     "group": "General"
   },
   {"name":"gtifoutPath",
    "label": "GTiff Output Path",
    "description": "Output path of the Gtiffs",
    "type": "string",
    "default": "",
    "group": "General"
  },
  {"name":"translatedPath",
   "label": "GTiff Translated Path",
   "description": "Translated path of the Gtiffs",
   "type": "string",
   "default": "",
   "group": "General"
 },
   {"name":"refreshInterval",
    "label": "Load Refresh Interval",
    "description": "Refresh Interval for loading GTiff",
    "type": "string",
    "default": "10",
    "group": "General"
   }
   ,
     {"name":"camera",
      "label": "Camera name phantom or flir",
      "description": "Camera name phantom or flir",
      "type": "string",
      "default": "flir",
      "group": "General"
     }

]
